# Повторная потеря печати после принятой сборки 213

25.09.2026 при проверке заявки 1317 обнаружен новый production API
`sha256:be2a182a2a1c3fe46417824022e82920d76fbb3624104a1c6f2d44780f0faf91`.
Его общий marketplace service побайтово совпал с версией до PR302.
Также исчезли шесть маршрутов печати переклейки и DI-регистрация сервиса.
Станция 2409 была онлайн, новые задания в очередь не попадали.

Пользователь уточнил: новый выпуск добавлял маршрут в WMS к исходному коробу.
Поэтому полного отката нет: все остальные 530 файлов нового API и весь новый web
сохраняются. APK 213 не меняется. Три восстановленных файла перечислены и защищены
начальными SHA-256 в `restore_runtime.py`. Их diff с принятой 213 проверен:
потерянные функции печати/пятёрки, wiring контроллера и providers; перестановка двух imports.

Новый `tsd-relabel-routes.spec.ts` проверяет наличие всех маршрутов, Nest DI и
передачу аргументов текущего пользователя. До восстановления все 7 тестов падали;
после проходят. На реальном кандидате проходят 33 проверки, включая прежние
проверки переклейки, печати, пятёрки, capability, Ozon и WB.

Общий API прогон: 2761 passed, 94 skipped. Отдельный
`kiz-duplicate.integration.spec.ts` исключён без тестовой БД.
Baseline guard: 8 passed. Android не менялся и не пересобирался.

Исходники этих функций уже содержали исправления; полный локальный dist не
публикуется. Изменения только в нашей WMS; проданная WMS не затрагивается.
Для следующих выпусков обязательно прогонять тест маршрутов на реальном кандидате,
а не только исходниках. Успех печати подтверждается отдельно на станции.
