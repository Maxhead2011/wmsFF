# KIZ history release 200

Only our WMS enables WMS_KIZ_REUSE_EVIDENCE_ENABLED; its web build enables VITE_KIZ_REUSE_EVIDENCE_ENABLED.
Existing sold flavors retain disabled behavior. No schema or inventory data migration.

The production source overlay preserves the currently deployed source-box guard, display cache,
branch billing dependencies and all previously published TSD features. The user explicitly approved
combining both conflicting sections. The planned-size predicate is self-contained for compatibility.

Validation: local API 2734 passed, 60 skipped; exact TSD candidate 1128 passed across six variants;
exact web candidate 172 passed plus typecheck/build; API candidate typecheck and KIZ regression tests.
Signed upgrade 199 to 200 preserves language, settings and pending operations.
Read-only live-data smoke returned REVIEW for the historical example; no forced relabel without proof.
True API configuration exists but circulation status was unavailable for the sample.

published-source.patch records the exact delta over the published API, web and Android sources.
The release retains older web assets and download files, and guards concurrent releases.
