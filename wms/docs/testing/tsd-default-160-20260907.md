# Promote LOGOFF TSD 160 to the default update channel

User authorized publishing the TSD update. No Android code or new APK build: promote the existing signed artifact from merged PR67 (`0.1.161-sorting-recovery`, code160), previously available only at its immutable URL. Sold FFULHAB remains untouched. Publishing the channel does not force installation or interrupt running devices.

## Scope

- `apps/web/public/downloads/logoff-tsd.json`: point to code160, immutable URL, exact size/hash.
- `apps/web/public/downloads/logoff-tsd.apk`: copy the verified existing signed build artifact (binary, no manual modification).
- `infra/tsd-default-update.Dockerfile`: promote the same already-live immutable APK to the default alias.
- `infra/scripts/tsd-default-update-release.sh`: pin current live web base, backup old channel/image, validate staged nginx delivery, require merged PR, publish only web and verify API/DB unchanged.
- `infra/scripts/tsd-default-update-verify.cjs`: pin expected APK hash, permit changes only to default APK/JSON.
- `infra/scripts/tsd-default-update.test.cjs`: current version/hash/default channel and real verifier CLI regression tests.

Risk: our default update channel becomes available to all LOGOFF TSD devices that request an update. No change to permissions, server logic, stock, Android flavor configuration, sold WMS or other APK channels. Existing code160 devices already have this build and need no reinstall; stock discrepancy fix was separately deployed through PR69.

## TDD and verification

RED: updated release test executed before manifest/APK changes: 2 failures (`158 !== 160`, old APK checksum), 1 existing safety test passed.
GREEN: 4/4 Node release tests PASS. `node --test --experimental-test-coverage infra/scripts/tsd-default-update.test.cjs`: verifier line/branch/function coverage 100%.

Local API: 152 files /1397 tests PASS. Web: 16 files /58 tests PASS; typecheck and bundle PASS (existing Vite asset/chunk warnings). Gradle `:app:testLogoffDebugUnitTest :app:testFfullhabDebugUnitTest --offline`: BUILD SUCCESSFUL, cached tasks UP-TO-DATE (52 tests per flavor from previous build). No new signing operation.

APK verified with aapt/apksigner:

- package `pro.logoff.wms.tsd`, minSdk24, versionCode160, versionName `0.1.161-sorting-recovery`;
- bytes 2255465;
- SHA256 `c6bdd49cf28aa630918326cab6e0a900a54a6b5946ab546fea1ada70d4796af5`;
- signing certificate SHA256 `52916d7797ade50cc1c50bba8787b9d2307b1e5dfd4ea725bd7c3be0e64f989b`.

Production staging/publication proof belongs in the PR comment after the release. Planned web base: `sha256:b6a6ee551de6e2b083c9e63c9083a701393a7beadc3ade326d9d00ab880f251c`. Backup path `/opt/logoff-wms-backups/tsd-default-160-20260907`. No database migration/write or API restart required. Physical installation is not claimed by these tests.

PR: `fix/tsd-default-update-160` → `fix/fbs-box-scan-route-consistency`, our integration branch. Unrelated dirty `apps/api/test/pallet-sorting-postgres.cjs` preserved/excluded. Failing checkpoint not committed per user's all-green-before-commit rule; RED/GREEN evidence retained here.
