# Сортировка и перемещение — локальная реализация, 07.09.2026

## Статус и границы

Ветка `feature/admin-pallet-sorting-20260907`, исходная точка `f2ad8a3`.
Production, реальные остатки и устройства сотрудников не изменялись. Миграция не применялась, резервная копия production в этой задаче не создавалась: записей в production не было.
PR и публикация требуют отдельного подтверждения.

При контрольном чтении remote `publish` релизная ветка нашей WMS `fix/fbs-box-scan-route-consistency` уже находилась на `796937e` (включает публикацию ТСД 158). Локальная `feature/our-vm` значительно старее и отсутствует среди одноимённых heads этого remote. Перед PR нужно согласовать актуальную базу и проверить совмещение с новыми перемещениями/актуализацией. Не публиковать весь старый checkout вместо актуального релиза.

## Реализованный сценарий

1. Только ADMIN с выбранным филиалом открывает сортировку по паллет-сорту или одному коробу.
2. Состав паллет-сорта фиксируется в сессии. Администратор сканирует присутствующие короба; повторный скан не добавляет товар.
3. Отсутствующие короба показываются вместе с остатками. Архивирование и списание требуют свежего снимка и отдельного подтверждения недостачи.
4. Администратор открывает новый пустой целевой короб и указывает его фактический паллет-сорт. Несуществующее расположение не придумывается.
5. ШК → КИЗ перемещает одну единицу: существующий парный MOVE −1/+1 и перенос привязки КИЗ в одной транзакции с состоянием сессии. Новая приёмка и отгрузка FBS не создаются.
6. «Закрыть короб» закрывает только текущий целевой короб. Можно открыть следующий.
7. «Завершить сортировку» показывает оставшиеся исходные остатки. Недостача списывается только после отдельного согласия; исходные короба архивируются и отсоединяются от паллетов через существующий сервис.
8. Доступные оставшиеся КИЗ получают BLOCKED и сохраняют ссылку на исторический исходный короб. Исторические PACKING/SHIPPING не меняются. Уже перемещённые КИЗ остаются в новых коробах.
9. Затронутые, ещё не отобранные задания FBS получают перестроение по текущему наличию в выбранном филиале. Физические сканы и выполненная сборка не сбрасываются. Заказ WB не отменяется, в WB ничего повторно не отправляется.

Сессия сохраняется на сервере после каждого действия. Версия сессии, уникальный номер команды, отпечаток запроса и уникальные ключи движений защищают повторы. После неизвестного сетевого результата UI предлагает повторить ту же команду. Список незавершённых сессий позволяет продолжить после перезапуска.

## Анализ влияния: файлы и функции

Пути относительно `wms/`. Новые файлы изолированы; существующие механизмы не рефакторились.

| Файлы | Функции / изменение | Риск и изоляция |
|---|---|---|
| `apps/api/prisma/schema.prisma`; `apps/api/prisma/migrations/20260907133000_admin_pallet_sorting/migration.sql` | Новая таблица PalletSortingSession, два индекса | Средний: нужна проверка миграции на отдельной БД; существующие таблицы не меняются |
| `apps/api/src/modules/inventory/pallet-sorting-policy.ts` | assertSortingAdmin, confirmSortingSnapshot, sortingKizIdentity, sortingTaskCanReroute | Ограничение роли, филиала, флага; неизвестные/опасные случаи запрещены |
| `apps/api/src/modules/inventory/dto/pallet-sorting.dto.ts`; `pallet-sorting.controller.ts` в той же inventory-папке | DTO и 7 новых маршрутов /pallet-sorting | Средний: серверная авторизация обязательна независимо от меню |
| `apps/api/src/modules/inventory/pallet-sorting.service.ts` | start, action, runAction, openTarget, move, previewInTx, archiveSources, resetAffectedRoutes, rebuildRoutes | Высокий: движение, списание, конкурентные операции; транзакции, проверки, отдельное согласие, история |
| `apps/api/src/modules/inventory/inventory.module.ts` | Регистрация контроллера/сервиса, импорт MarketplaceConnectionsModule | Средний: проверить DI при запуске тестового сервера |
| `apps/api/src/modules/stock/stock-operations.service.ts` | Только новый transferSortingUnit; reuse resolveStorageBoxTransferItem/applyTransferBetweenBoxes | Высокий: общий складской модуль, старые точки входа не изменены |
| `apps/api/src/modules/marketplace-connections/marketplace-connections.service.ts` | Новый repairFbsSortingAutomaticTasks; необязательный onlyTaskIds у repairFbsRequestSelection | Высокий: FBS; новый режим под ADMIN+флагом, без захвата чужих AUTO-задач и без изменений обычного вызова |
| `apps/api/src/modules/administration/administration-internal-api.service.ts` | Существующая группа inventory включает новый API и его описание | Низкий: только каталог, тест количества маршрутов |
| `apps/web/src/lib/workspaces.ts`; `apps/web/src/App.tsx` | Новый WorkspaceId, меню и рендер | Низкий: VITE-флаг и ADMIN до обхода system:admin |
| `apps/web/src/lib/pallet-sorting-api.ts` | sortingRequest, типы состояния/снимка | Изолированный клиент API; большой api.ts не меняется |
| `apps/web/src/components/inventory/PalletSortingPanel.tsx`; `pallet-sorting.css` | Один экран, модальное согласие, повторы, прокрутка | Средний: протестировано в браузере с синтетическим API |
| `apps/android-tsd/app/src/main/java/pro/logoff/wms/tsd/PalletSortingScreen.java`; `PalletSortingCommand.java` | Новый нативный экран и сохранение неподтверждённой команды | Средний: нужен физический пилот; существующие экраны отдельно |
| `apps/android-tsd/app/src/main/java/pro/logoff/wms/tsd/MainActivity.java` | Меню, scanner dispatch, back/refresh для нового экрана | Средний: общий сканер; меню только logoff+ADMIN, heartbeat не стирает ввод нового экрана |
| `apps/android-tsd/app/src/main/java/pro/logoff/wms/tsd/network/WmsApi.java` | Три метода API новой сортировки | Низкий: существующие методы без изменений |
| `apps/api/test/pallet-sorting-policy.spec.ts`; `pallet-sorting-session.spec.ts`; `pallet-sorting-route.spec.ts`; `pallet-sorting-stock.spec.ts` | Новые регрессионные тесты | Проверки с mock БД, не доказательство SQL rollback |
| `apps/web/src/components/inventory/PalletSortingPanel.spec.tsx`; `apps/web/test/pallet-sorting-browser.mjs`; `pallet-sorting-fixture.html`; `pallet-sorting-fixture.tsx` в web/test | Unit и реально запущенный браузерный сценарий | Только синтетические данные; fixture не входит в production entry |
| `apps/android-tsd/app/src/test/java/pro/logoff/wms/tsd/PalletSortingCommandTest.java` | Повтор сетевого запроса и запрет второй команды | Unit, без физического сканера |

Новые участки снабжены ADDED/FIX, тесты — TEST. В JSX/CSS/SQL используется соответствующий языку формат комментария.

## Защита проданной WMS

- API выключен по умолчанию: `WMS_PALLET_SORTING_ENABLED` должен явно равняться `true`.
- Веб-меню выключено по умолчанию: `VITE_PALLET_SORTING_ENABLED=true` только при сборке нашей WMS.
- Android-меню доступно только flavor `logoff` и роли ADMIN; во flavor `ffullhab` скрыто.
- Ни один флаг на сервере не менялся. Compile sold-flavor — дополнительная проверка, не гарантия runtime-совместимости.
- Общие файлы stock/marketplace/MainActivity обязательно указать в предупреждении PR. Новую таблицу/флаги проданной инсталляции не включать.

## TDD и проверки

RED до реализации: отсутствующие policy/service/transfer/Java-класс и React-компонент; чрезмерно широкий выбор заданий FBS; отсутствие обработки AUTO.
Дополнительные RED во время проверки: изменение исторических PACKING-меток при архивировании, запись с чужим клиентом, незавершённая актуализация MISMATCH, активная не-FBS заявка, недоступный повтор потерянного ответа внутри модального окна, подтверждение несохранённого маршрута при CAS count=0, уход физической сортировки в бескоробный расчёт.
После исправлений соответствующие тесты прошли. Ошибки неполных mock-объектов сначала исправлялись в тестах и не считались доказательством дефекта.

- API: 1069 тестов / 136 файлов прошли; lint = `tsc --noEmit`, build = `tsc` прошли. Из них 27 новых тестов сортировки.
- Web: 47 тестов / 13 файлов; lint, TypeScript и Vite production build прошли.
- Android: 40 JUnit-тестов, `assembleLogoffDebug`, `compileFfullhabDebugJavaWithJavac` прошли.
- `prisma validate` прошёл с фиктивным локальным URL; соединения с production не было.
- Edge headless, Vite на 127.0.0.1, 1440×900 и 375×812: скан исходного короба, отмена/согласие на недостачу, закрытие короба отдельно от завершения, неверный КИЗ очищается, reload/resume, вертикальная и горизонтальная прокрутка, реальный modal, потеря ответа после MOVE и после списания с безопасным повтором. Итог: 1 перемещение, 2 подтверждённых этапа списания, 0 pageerror.
- Скриншоты хранятся локально вне Git. Это тест интерфейса с перехваченными API-ответами, не полный E2E на PostgreSQL.
- Покрытие в процентах не измерено: coverage-provider в окружении отсутствует.
- Vite сообщил предупреждения о размере bundle и отсутствующих при сборке статических шрифтах/изображении; Android — deprecated API. Сборки успешны, эти соседние участки не изменялись.

## Ограничения и обязательные шаги до включения

1. Совместить с актуальной релизной веткой через PR. Конфликты не разрешать автоматически; после согласованного совмещения повторить полный прогон.
2. На отдельной тестовой PostgreSQL проверить запуск Nest/DI, применение additive migration, реальный MOVE −1/+1, атомарный rollback после ошибки КИЗ, параллельные администратор/сборщик, одинаковый operationId и рестарт сервера.
3. Проверить FBS до/после перемещения: источник обновлён, уже собранное не сброшено, нет второй отгрузки, нет прибавления остатка. При конфликте сохранения маршрут остаётся на повторе.
4. Незавершённые перестроения хранятся в сессии и повторяются при действии/возобновлении в вебе или кнопкой в ТСД. Автономного фонового worker для этой очереди нет.
5. Положительные складские остатки RESERVED/PACKING и прочие не-AVAILABLE, полная инвентаризация, COUNTING/MISMATCH и активная не-FBS заявка не обходятся. Логические задания FBS перестраиваются, но это не разрешение стереть чужое физическое действие.
6. Для неизвестного КИЗ при нескольких возможных источниках требуется указать исходный короб на том же экране. Излишек сверх учтённого AVAILABLE автоматически не приходуется: нужна отдельная актуализация. Новый сервис не заменяет весь модуль инвентаризации.
7. Большой паллет требует нагрузочной проверки: операция архивирования целиком транзакционная с timeout 30 секунд, разбиение на пакеты не внедрено.
8. Нужен пилот на одном согласованном ТСД. Собран только debug APK на исходной базе этой ветки: не заменять им текущий release 158. Подписанный APK/версия/канал обновления в задаче не менялись.
9. Перед разрешённым production-внедрением сделать свежую проверяемую резервную копию БД и текущих артефактов. Сначала миграция/код с выключенными флагами, затем проверка и включение для нашей системы. Отключение флага останавливает новые действия, но не откатывает уже совершённые складские движения; обратные операции требуют отдельного разбора.

Публикация до этих проверок не рекомендуется. Реальные короба, КИЗ, заявки и учётные данные не включены в новые фикстуры.
