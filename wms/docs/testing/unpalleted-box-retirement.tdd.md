# TDD: списание коробов без паллет-сорта

## Источник требований

Требования получены непосредственно от пользователя для клиента `c76b78f9-1b83-4e9b-bee3-bc28336ee1c9`.

## Пользовательский сценарий

Администратор должен предварительно увидеть безопасные и заблокированные короба целевого клиента, а затем подтверждённо списать не более 25 безопасных коробов. Клиент и другие роли не должны иметь доступа к операции.

## RED / GREEN

| Этап | Команда | Результат |
|---|---|---|
| RED | `vitest run test/administration-unpalleted-writeoff.service.spec.ts` | FAIL: ожидаемо отсутствовал `administration-unpalleted-writeoff.service.ts` |
| GREEN API | целевые specs списания, mobile, integration, turnover, размещения, техработ и FBS | PASS: 54/54 проверки задачи |
| GREEN web | `vitest run` из `apps/web` | PASS: 9/9 тестов, включая 4/4 проверки ADMIN-only UI и границы пакета |
| TypeScript | `tsc -p apps/api/tsconfig.json --noEmit` и `tsc -p apps/web/tsconfig.json --noEmit` | PASS |
| Build | API `tsc` и web `vite build` | PASS; только существующие предупреждения о runtime assets и размере общего chunk |
| Полный API suite | `vitest run` | 453 PASS / 74 FAIL; число прежних падений не выросло, добавленные проверки задачи зелёные |

## Гарантии тестов

| # | Гарантия | Тест | Тип | Результат |
|---|---|---|---|---|
| 1 | Оба HTTP-метода защищены декоратором `system:admin` | `защищает preview и apply...` | security unit | PASS |
| 2 | Preview не выполняет транзакции и исключает code-only размещение без учёта регистра | `показывает только реально непривязанные короба...` | unit | PASS |
| 3 | PACKING, незавершённая заявка, FBS-сборка и открытая инвентаризация блокируют списание | тот же тест | unit | PASS |
| 4 | Сервис повторно запрещает доступ клиенту и demo-admin даже при наличии `system:admin` | `запрещает анализ и списание...` | security unit | PASS |
| 5 | AVAILABLE списывается отрицательным движением, AVAILABLE-КИЗы блокируются, короб архивируется | `списывает AVAILABLE...` | transactional unit | PASS |
| 6 | Требуются точная фраза; ровно 25 ID проходят границу, 26 отклоняются | `требует точное подтверждение...`, `принимает граничную партию...` | validation unit | PASS |
| 7 | Глобальная инвентаризация останавливает операцию до транзакции и при гонке внутри транзакции | `не открывает транзакции...`, `останавливает короб внутри транзакции...` | safety/race unit | PASS |
| 8 | Появившийся после preview паллет-сорт отменяет списание | `пропускает короб...` | race unit | PASS |
| 9 | Повторный запуск не создаёт второе движение | `повторный запуск...` | idempotency unit | PASS |
| 10 | Сбой транзакции одного короба не скрывает уже успешно обработанный короб | `изолирует ошибку одного короба...` | resilience unit | PASS |
| 11 | Служебное движение скрыто из товарооборота и внешнего API клиента | `turnover-unpalleted...`, `integration-api-scope...` | privacy unit | PASS |
| 12 | Клиент и demo-admin не получают плитку, настоящий system:admin получает | `AdministrationUnpalletedWriteoff.spec.ts` | UI access unit | PASS |
| 13 | Чужие связи/FBS, несовпадение КИЗ, повторно запускаемая FAILED-волна и незакрытая проверка блокируют короб | `блокирует чужие остатки...`, `не списывает короб при транзакционном блокере...` | safety unit | PASS |
| 14 | Несовпадение КИЗ, появившееся после preview, повторно блокирует применение без записей | `не меняет остаток...` | transactional unit | PASS |
| 15 | Неуспешная транзакция создаёт отдельную запись аудита без сырого текста БД | `изолирует ошибку одного короба...` | audit unit | PASS |
| 16 | Мобильный товарооборот клиента скрывает служебное движение | `скрывает внутреннее списание...` | privacy unit | PASS |
| 17 | Mobile stock/dashboard/catalog/warehouse Лукина показывают только активные короба на паллет-сортах | `скрывает/не считает непривязанные короба...` | visibility unit | PASS |
| 18 | Обычное и техническое размещение блокируют строку Box; архивный короб не размещается | `блокирует активный короб...`, `не размещает архивный короб...`, `блокирует строку короба...` | race unit | PASS |
| 19 | Физический, фоновый и manager-repair FBS резервируют только активный короб точного филиала с живым остатком; CAS и retry `P2034` закрывают гонки | `accepts a required FBS box...`, `rechecks active placement...`, `serialization conflict`, `strict CAS`, `legacy FBS request`, `AUTO FBS box` | race unit | PASS |
| 20 | Integration `/stocks` Лукина не возвращает непривязанный остаток | `hides unplaced Lukin boxes...` | privacy unit | PASS |
| 21 | Основной web-товарооборот и карточка короба Лукина не показывают непривязанные остатки и КИЗы | `скрывает непривязанные остатки...`, `не открывает клиенту...` | privacy unit | PASS |
| 22 | Инвентаризация, открытая между коробами, прекращает пакет до следующей транзакции и возвращает стабильную причину | `не открывает транзакцию следующего короба...` | race/audit unit | PASS |
| 23 | Suggestions товарооборота и mixed CLIENT-роли не обходят фильтр непривязанных остатков | `turnover-unpalleted...`, `mobile-unpalleted...` | privacy unit | PASS |
| 24 | Web отправляет всю разрешённую партию из 25 коробов | `отправляет до 25 коробов...` | UI boundary unit | PASS |

## Известные ограничения проверки

Coverage-команда не запускалась: в workspace отсутствует пакет `@vitest/coverage-v8`; новые зависимости без отдельного согласования не устанавливались.

Полный набор API-тестов исходного production-снимка не был зелёным до этой задачи: старые моки не учитывают филиалы и новые зависимости сервисов. Эти падения не исправлялись, чтобы не расширять область изменения. Встроенный реестр API до задачи отставал на шесть маршрутов; два маршрута данной задачи учтены отдельно.
